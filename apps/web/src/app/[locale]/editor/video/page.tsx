import { VideoEditor } from "@/components/editor/VideoEditor";

export const metadata = {
  title: "Video Editor - Media Editor",
};

export default function VideoEditorPage() {
  return <VideoEditor />;
}
